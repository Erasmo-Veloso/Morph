{
  "name": "morph",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit --pretty false",
    "validate": "npm run typecheck && tsx scripts/validate-contract.ts && node scripts/validate.mjs && node scripts/validate-realtime.mjs && node scripts/validate-session-persistence.mjs",
    "start": "node server/index.mjs"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
